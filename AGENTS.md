## Cursor Cloud specific instructions

- This checkout currently contains no application source, dependency manifests, lockfiles, or documented dev commands beyond the root `README.md` title.
- Do not assume the stale backend/frontend npm commands exist unless matching `package.json` files are present; use the manifests and docs that are committed with any future app code.
- There are currently no runnable services to start, lint, test, build, or exercise end to end from this checkout.
